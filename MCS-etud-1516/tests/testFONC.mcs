int foo (int x, int y) {
   int z = x * y;
   return 4;
}

int nader(int x, int y , int z , int f, int g) {
 int h = f*y;
 return 5;
}

int main(){
    foo(1,2);
    nader(4,5,6,7,8);
    return 0;
}
